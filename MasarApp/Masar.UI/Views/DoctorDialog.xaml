<Window x:Class="Masar.UI.Views.DoctorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Title.Doctor}" Height="500" Width="640" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Border Style="{StaticResource CardStyle}" Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource Title.Doctor}" FontSize="18" FontWeight="SemiBold" />

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,12,12">
                        <TextBlock Text="{DynamicResource Label.FullName}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Doctor.FullName}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Label.Rank}" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding Ranks}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding SelectedRank}" />
                    </StackPanel>

                    <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,12,12">
                        <TextBlock Text="{DynamicResource Label.Qualification}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Doctor.Qualification}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Label.Gender}" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding Genders}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding SelectedGender}" />
                    </StackPanel>

                    <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,0,12,12">
                        <TextBlock Text="{DynamicResource Label.Phone}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Doctor.Phone}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Grid.Row="2" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Label.Email}" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Doctor.Email}" />
                    </StackPanel>

                    <StackPanel Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Label.College}" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding Colleges}" DisplayMemberPath="NameEn" SelectedValuePath="CollegeId" SelectedValue="{Binding SelectedCollegeId}" />
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="{DynamicResource Button.Save}" Command="{Binding SaveCommand}" Margin="0,0,8,0" />
                <Button Content="{DynamicResource Button.Cancel}" Command="{Binding CancelCommand}" Style="{StaticResource GhostButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
