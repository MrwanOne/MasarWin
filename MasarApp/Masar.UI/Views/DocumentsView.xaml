<UserControl x:Class="Masar.UI.Views.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Masar.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             AllowDrop="True" Drop="UserControl_Drop">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="{DynamicResource Title.Documents}" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="{DynamicResource Subtitle.Documents}" Foreground="{StaticResource MutedTextBrush}" Margin="0,5,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,1,1,1" CornerRadius="10">
                <DataGrid ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}" 
                          AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" BorderThickness="0">
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding PreviewCommand}" />
                    </DataGrid.InputBindings>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Grid.FileName}" Binding="{Binding FileName}" Width="2*" />
                        <DataGridTextColumn Header="{DynamicResource Grid.Version}" Binding="{Binding Version}" Width="50" />
                        <DataGridTextColumn Header="{DynamicResource Grid.Size}" Binding="{Binding FileSize, Converter={StaticResource FileSizeConverter}}" Width="100" />
                        <DataGridTextColumn Header="{DynamicResource Grid.ModifiedAt}" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150" />
                        <DataGridTextColumn Header="{DynamicResource Grid.Status}" Binding="{Binding Status}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Drag & Drop Overlay -->
            <Border x:Name="DropOverlay" Background="#401E3A8A" CornerRadius="10" Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📥" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="اسحب الملفات هنا للرفع" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Loading Overlay -->
            <Border Background="#80FFFFFF" CornerRadius="10" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4" />
                    <TextBlock Text="{DynamicResource Status.Loading}" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="{DynamicResource Button.Delete}" Command="{Binding DeleteCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0"/>
            <Button Content="{DynamicResource Button.Download}" Command="{Binding DownloadCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0"/>
            <Button Content="{DynamicResource Button.Preview}" Command="{Binding PreviewCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0"/>
            <Button Content="{DynamicResource Button.Upload}" Command="{Binding UploadCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
