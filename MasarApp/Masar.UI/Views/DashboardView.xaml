<UserControl x:Class="Masar.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel>
        <Border Style="{StaticResource CardStyle}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="{DynamicResource Title.Dashboard}" FontSize="22" FontWeight="SemiBold" Foreground="#0F172A" />
                    <TextBlock Text="{DynamicResource Subtitle.Dashboard}" Foreground="{StaticResource MutedTextBrush}" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Border Width="40" Height="40" CornerRadius="10" Background="#1E3A8A" Margin="0,0,12,0">
                        <Image Source="/Masar.UI;component/Resources/Images/logo.png"
                        Width="40" Height="40" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <Button Content="{DynamicResource Button.Refresh}" Command="{Binding RefreshCommand}" Style="{StaticResource GhostButtonStyle}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Margin="0,4,0,0" Background="#F1F5F9">
            <StackPanel Orientation="Horizontal">
                <Border Background="#1E3A8A" CornerRadius="4" Padding="8,4" Margin="0,0,12,0">
                    <TextBlock Text="{DynamicResource Label.ActiveTerm}" FontWeight="Bold" Foreground="White" />
                </Border>
                <TextBlock Text="{Binding ActiveTermName}" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="#1E3A8A" FontSize="16" />
                <TextBlock Text=" - " VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" Margin="8,0" />
                <TextBlock Text="{Binding ActiveTermDates}" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
        </Border>

        <UniformGrid Columns="3">
            <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding ShowAllProjectsCommand}" Padding="0" BorderThickness="0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.TotalProjects}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding TotalProjects}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Button>
            <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding ShowProposedProjectsCommand}" Padding="0" BorderThickness="0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.ProposedProjects}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding ProposedProjects}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Button>
            <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding ShowApprovedProjectsCommand}" Padding="0" BorderThickness="0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.ApprovedProjects}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding ApprovedProjects}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Button>
        </UniformGrid>

        <UniformGrid Columns="3">
            <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding ShowCompletedProjectsCommand}" Padding="0" BorderThickness="0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.CompletedProjects}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding CompletedProjects}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Button>
            <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding ShowStudentsCommand}" Padding="0" BorderThickness="0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.TotalStudents}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding TotalStudents}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Button>
            <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding ShowCommitteesCommand}" Padding="0" BorderThickness="0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard.TotalCommittees}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding TotalCommittees}" FontSize="24" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </Button>
        </UniformGrid>
    </StackPanel>
</UserControl>
