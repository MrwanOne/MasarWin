<UserControl x:Class="Masar.UI.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource Nav.AuditLog}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="مراجعة جميع العمليات على النظام" Foreground="#B0BEC5" FontSize="14"/>
                </StackPanel>
                <Button Grid.Column="1" Content="🔄 تحديث" Command="{Binding RefreshCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" Padding="15" Margin="20,10,20,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="نوع الكيان" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding EntityTypes}" 
                              SelectedItem="{Binding SelectedEntityType}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="المستخدم" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SearchUsername, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="من تاريخ" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding DateFrom}"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="إلى تاريخ" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding DateTo}"/>
                </StackPanel>

                <Button Grid.Column="4" Content="مسح الفلاتر" Command="{Binding ClearFiltersCommand}"
                        VerticalAlignment="Bottom" Margin="0,0,0,0"/>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" Margin="20,10,20,20" Padding="10">
            <Grid>
                <!-- Loading Overlay -->
                <Border Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                        Panel.ZIndex="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="جاري التحميل..." Foreground="White" FontSize="18"/>
                    </StackPanel>
                </Border>

                <DataGrid ItemsSource="{Binding AuditLogs}" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal" AlternatingRowBackground="#F5F5F5"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding ChangedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150"/>
                        <DataGridTextColumn Header="العملية" Binding="{Binding ActionDisplay}" Width="80"/>
                        <DataGridTextColumn Header="الكيان" Binding="{Binding EntityName}" Width="120"/>
                        <DataGridTextColumn Header="معرف" Binding="{Binding EntityId}" Width="80"/>
                        <DataGridTextColumn Header="المستخدم" Binding="{Binding Username}" Width="120"/>
                        <DataGridTemplateColumn Header="القيم السابقة" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding OldValues}" TextWrapping="Wrap" Padding="4,6" VerticalAlignment="Top" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="القيم الجديدة" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NewValues}" TextWrapping="Wrap" Padding="4,6" VerticalAlignment="Top" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
