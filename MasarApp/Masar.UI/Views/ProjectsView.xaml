<UserControl x:Class="Masar.UI.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Masar.UI.Controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Grid.Row="0">
            <StackPanel>
                <DockPanel>
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="{DynamicResource Title.ProjectManagement}" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource Subtitle.Projects}" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="12,0,0,0">
                        <TextBox Width="220" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                        <Button Content="{DynamicResource Button.Search}" Command="{Binding SearchCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" />
                        <Button Content="{DynamicResource Button.Refresh}" Command="{Binding RefreshCommand}" Style="{StaticResource GhostButtonStyle}" />
                    </StackPanel>
                </DockPanel>
                <WrapPanel Margin="0,12,0,0" HorizontalAlignment="Left">
                    <Button Content="{DynamicResource Button.Add}" Command="{Binding AddCommand}" Margin="0,0,8,0" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{DynamicResource Button.Edit}" Command="{Binding EditCommand}" Margin="0,0,8,0" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{DynamicResource Button.Delete}" Command="{Binding DeleteCommand}" Margin="0,0,8,0" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{DynamicResource Button.Accept}" Command="{Binding AcceptCommand}" Margin="0,0,8,0" Style="{StaticResource SecondaryButtonStyle}" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{DynamicResource Button.Reject}" Command="{Binding RejectCommand}" Margin="0,0,8,0" Style="{StaticResource GhostButtonStyle}" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{DynamicResource Button.AssignSupervisor}" Command="{Binding AssignSupervisorCommand}" Margin="0,0,8,0" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="📜 سجل التغييرات" Command="{Binding ViewHistoryCommand}" Margin="0,0,8,0" Style="{StaticResource GhostButtonStyle}" />
                    <Button Content="{DynamicResource Button.UploadDoc}" Command="{Binding OpenDocumentsCommand}" Margin="0,0,8,0" Style="{StaticResource GhostButtonStyle}" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding PagedItems}" SelectedItem="{Binding SelectedProject}" Margin="0,12,0,12" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource Grid.College}" Binding="{Binding CollegeName}" Width="*" />
                <DataGridTextColumn Header="{DynamicResource Grid.Title}" Binding="{Binding Title}" Width="2*" />
                <DataGridTextColumn Header="{DynamicResource Grid.Department}" Binding="{Binding DepartmentName}" Width="*" />
                <DataGridTextColumn Header="{DynamicResource Grid.Team}" Binding="{Binding TeamName}" Width="*" />
                <DataGridTextColumn Header="{DynamicResource Grid.Beneficiary}" Binding="{Binding Beneficiary}" Width="*" />
                <DataGridTextColumn Header="{DynamicResource Grid.Supervisor}" Binding="{Binding SupervisorName}" Width="*" />
                <DataGridTemplateColumn Header="{DynamicResource Grid.Status}" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:StatusBadge Status="{Binding Status}" HorizontalAlignment="Center" Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="{DynamicResource Grid.Completion}" Binding="{Binding CompletionRate, StringFormat={}{0:0}%}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Button Content="{DynamicResource Button.Previous}" Command="{Binding PreviousPageCommand}" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" />
            <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" FontWeight="SemiBold" />
            <TextBlock Text="/" Margin="4,0" VerticalAlignment="Center" />
            <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" />
            <Button Content="{DynamicResource Button.Next}" Command="{Binding NextPageCommand}" Style="{StaticResource GhostButtonStyle}" Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
