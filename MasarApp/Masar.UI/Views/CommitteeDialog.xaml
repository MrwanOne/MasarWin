<Window x:Class="Masar.UI.Views.CommitteeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="{DynamicResource Title.Committee}" Height="550" Width="650" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Border Style="{StaticResource CardStyle}" Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource Title.CommitteeDetails}" FontSize="18" FontWeight="SemiBold" />

            <!-- Committee Info -->
            <Grid Grid.Row="1" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,12,8">
                    <TextBlock Text="{DynamicResource Label.CommitteeName}" />
                    <TextBox Text="{Binding Committee.Name}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="{DynamicResource Label.College}" />
                    <ComboBox ItemsSource="{Binding Colleges}" DisplayMemberPath="NameAr" SelectedValuePath="CollegeId" SelectedValue="{Binding SelectedCollegeId}" />
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1">
                    <TextBlock Text="{DynamicResource Label.Department}" />
                    <ComboBox ItemsSource="{Binding Departments}" DisplayMemberPath="NameAr" SelectedValuePath="DepartmentId" SelectedValue="{Binding SelectedDepartmentId}" />
                </StackPanel>
            </Grid>

            <!-- Doctor Members Selection -->
            <GroupBox Grid.Row="2" Header="{DynamicResource Label.SelectMembers}" Margin="0,12,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding AvailableDoctors}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}" Margin="4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" Width="200" />
                                        <TextBlock Text="{Binding DepartmentName}" Foreground="Gray" />
                                    </StackPanel>
                                </CheckBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </GroupBox>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="{DynamicResource Button.Save}" Command="{Binding SaveCommand}" Margin="0,0,8,0" />
                <Button Content="{DynamicResource Button.Cancel}" Command="{Binding CancelCommand}" Style="{StaticResource GhostButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
