<UserControl x:Class="Masar.UI.Views.ReportsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Filters Panel -->
        <Border Style="{StaticResource CardStyle}" Padding="20">
            <StackPanel>
                <!-- Header -->
                <TextBlock Text="{DynamicResource Title.Reports}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBlock Text="{DynamicResource Subtitle.Reports}" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,20" />

                <!-- Filter Fields -->
                <Grid Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Row 1 -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                        <TextBlock Text="{DynamicResource Label.College}" Margin="0,0,0,6" FontWeight="Medium" />
                        <ComboBox ItemsSource="{Binding Colleges}" DisplayMemberPath="NameEn" SelectedValuePath="CollegeId" SelectedValue="{Binding SelectedCollegeId}" Padding="10,8" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Label.Department}" Margin="0,0,0,6" FontWeight="Medium" />
                        <ComboBox ItemsSource="{Binding FilteredDepartments}" DisplayMemberPath="NameEn" SelectedValuePath="DepartmentId" SelectedValue="{Binding SelectedDepartmentId}" Padding="10,8" />
                    </StackPanel>

                    <!-- Row 2 -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,12">
                        <TextBlock Text="{DynamicResource Label.Title}" Margin="0,0,0,6" FontWeight="Medium" />
                        <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
                        <TextBlock Text="{DynamicResource Label.Supervisor}" Margin="0,0,0,6" FontWeight="Medium" />
                        <ComboBox ItemsSource="{Binding Supervisors}" DisplayMemberPath="FullName" SelectedValuePath="DoctorId" SelectedValue="{Binding SelectedSupervisorId}" Padding="10,8" />
                    </StackPanel>

                    <!-- Row 3 -->
                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="{DynamicResource Label.Status}" Margin="0,0,0,6" FontWeight="Medium" />
                        <ComboBox ItemsSource="{Binding StatusOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding SelectedStatus}" Padding="10,8" />
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBlock Text="{DynamicResource Label.Year}" Margin="0,0,0,6" FontWeight="Medium" />
                        <ComboBox ItemsSource="{Binding YearOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding SelectedYear}" Padding="10,8" />
                    </StackPanel>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{DynamicResource Button.GeneratePreview}" Command="{Binding GenerateReportCommand}" Padding="24,10" FontWeight="Medium" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Preview Section -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20">
            <StackPanel>
                <TextBlock Text="{DynamicResource Label.Preview}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBlock Text="{DynamicResource Subtitle.ReportPreview}" Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
