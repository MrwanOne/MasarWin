<Window x:Class="Masar.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Masar.UI.Controls"
        mc:Ignorable="d"
        Title="{DynamicResource App.LoginTitle}" Height="600" Width="900" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0" />
                <GradientStop Color="#1E293B" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="48" VerticalAlignment="Center">
            <Border Width="220" Height="220" CornerRadius="24" Background="#1E3A8A">
                <Image Source="/Masar.UI;component/Resources/Images/logo.png"
                       Width="220" Height="220" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Border>
            <TextBlock Text="{DynamicResource App.Name}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,20,0,0" />
            <TextBlock Text="{DynamicResource App.SystemSubtitle}" Foreground="#94A3B8" FontSize="14" Margin="0,8,0,0" />
            <TextBlock Text="{DynamicResource Login.WelcomeBack}" Foreground="#CBD5F5" FontSize="12" Margin="0,24,0,0" TextWrapping="Wrap" Width="320" />
        </StackPanel>

        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="48" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="{DynamicResource Login.SignIn}" FontSize="22" FontWeight="SemiBold" Foreground="#0F172A" />
                <TextBlock Text="{DynamicResource Login.UseAssignedAccount}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,12" />

                <TextBlock Text="{DynamicResource Login.Username}" Foreground="#1E293B" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="280" HorizontalAlignment="Left" />

                <TextBlock Text="{DynamicResource Login.Password}" Margin="0,12,0,0" Foreground="#1E293B" />
                <PasswordBox controls:PasswordBoxAssistant.BindPassword="True"
                             controls:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Width="280" HorizontalAlignment="Left" />

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" Margin="0,8,0,0" TextWrapping="Wrap" />

                <Button Content="{DynamicResource Login.Button}" Margin="0,16,0,0" Command="{Binding LoginCommand}" />

                <TextBlock Text="{DynamicResource Login.DefaultAdmin}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,16,0,0" />
            </StackPanel>
        </Border>
        <Button Content="{DynamicResource Language.Toggle}" Command="{Binding ToggleLanguageCommand}"
                Style="{StaticResource GhostButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,16,16,0" />
    </Grid>
</Window>
